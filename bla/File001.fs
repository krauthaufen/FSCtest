namespace Some
open System
module File001 =
    let list =
        let rand = Random()
        [
            if rand.NextDouble() > 0.5 then "00001"
            if rand.NextDouble() > 0.5 then "00002"
            if rand.NextDouble() > 0.5 then "00003"
            if rand.NextDouble() > 0.5 then "00004"
            if rand.NextDouble() > 0.5 then "00005"
            if rand.NextDouble() > 0.5 then "00006"
            if rand.NextDouble() > 0.5 then "00007"
            if rand.NextDouble() > 0.5 then "00008"
            if rand.NextDouble() > 0.5 then "00009"
            if rand.NextDouble() > 0.5 then "00010"
            if rand.NextDouble() > 0.5 then "00011"
            if rand.NextDouble() > 0.5 then "00012"
            if rand.NextDouble() > 0.5 then "00013"
            if rand.NextDouble() > 0.5 then "00014"
            if rand.NextDouble() > 0.5 then "00015"
            if rand.NextDouble() > 0.5 then "00016"
            if rand.NextDouble() > 0.5 then "00017"
            if rand.NextDouble() > 0.5 then "00018"
            if rand.NextDouble() > 0.5 then "00019"
            if rand.NextDouble() > 0.5 then "00020"
            if rand.NextDouble() > 0.5 then "00021"
            if rand.NextDouble() > 0.5 then "00022"
            if rand.NextDouble() > 0.5 then "00023"
            if rand.NextDouble() > 0.5 then "00024"
            if rand.NextDouble() > 0.5 then "00025"
            if rand.NextDouble() > 0.5 then "00026"
            if rand.NextDouble() > 0.5 then "00027"
            if rand.NextDouble() > 0.5 then "00028"
            if rand.NextDouble() > 0.5 then "00029"
            if rand.NextDouble() > 0.5 then "00030"
            if rand.NextDouble() > 0.5 then "00031"
            if rand.NextDouble() > 0.5 then "00032"
            if rand.NextDouble() > 0.5 then "00033"
            if rand.NextDouble() > 0.5 then "00034"
            if rand.NextDouble() > 0.5 then "00035"
            if rand.NextDouble() > 0.5 then "00036"
            if rand.NextDouble() > 0.5 then "00037"
            if rand.NextDouble() > 0.5 then "00038"
            if rand.NextDouble() > 0.5 then "00039"
            if rand.NextDouble() > 0.5 then "00040"
        ]
