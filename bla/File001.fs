namespace Some
open System
module File001 =
    let list =
        let rand = Random()
        [
            if rand.NextDouble() > 0.5 then yield "00001"
            if rand.NextDouble() > 0.5 then yield "00002"
            if rand.NextDouble() > 0.5 then yield "00003"
            if rand.NextDouble() > 0.5 then yield "00004"
            if rand.NextDouble() > 0.5 then yield "00005"
            if rand.NextDouble() > 0.5 then yield "00006"
            if rand.NextDouble() > 0.5 then yield "00007"
            if rand.NextDouble() > 0.5 then yield "00008"
            if rand.NextDouble() > 0.5 then yield "00009"
            if rand.NextDouble() > 0.5 then yield "00010"
            if rand.NextDouble() > 0.5 then yield "00011"
            if rand.NextDouble() > 0.5 then yield "00012"
            if rand.NextDouble() > 0.5 then yield "00013"
            if rand.NextDouble() > 0.5 then yield "00014"
            if rand.NextDouble() > 0.5 then yield "00015"
            if rand.NextDouble() > 0.5 then yield "00016"
            if rand.NextDouble() > 0.5 then yield "00017"
            if rand.NextDouble() > 0.5 then yield "00018"
            if rand.NextDouble() > 0.5 then yield "00019"
            if rand.NextDouble() > 0.5 then yield "00020"
            if rand.NextDouble() > 0.5 then yield "00021"
            if rand.NextDouble() > 0.5 then yield "00022"
            if rand.NextDouble() > 0.5 then yield "00023"
            if rand.NextDouble() > 0.5 then yield "00024"
            if rand.NextDouble() > 0.5 then yield "00025"
            if rand.NextDouble() > 0.5 then yield "00026"
            if rand.NextDouble() > 0.5 then yield "00027"
            if rand.NextDouble() > 0.5 then yield "00028"
            if rand.NextDouble() > 0.5 then yield "00029"
            if rand.NextDouble() > 0.5 then yield "00030"
            if rand.NextDouble() > 0.5 then yield "00031"
            if rand.NextDouble() > 0.5 then yield "00032"
            if rand.NextDouble() > 0.5 then yield "00033"
            if rand.NextDouble() > 0.5 then yield "00034"
            if rand.NextDouble() > 0.5 then yield "00035"
            if rand.NextDouble() > 0.5 then yield "00036"
            if rand.NextDouble() > 0.5 then yield "00037"
            if rand.NextDouble() > 0.5 then yield "00038"
            if rand.NextDouble() > 0.5 then yield "00039"
            if rand.NextDouble() > 0.5 then yield "00040"
            if rand.NextDouble() > 0.5 then yield "00041"
            if rand.NextDouble() > 0.5 then yield "00042"
            if rand.NextDouble() > 0.5 then yield "00043"
            if rand.NextDouble() > 0.5 then yield "00044"
            if rand.NextDouble() > 0.5 then yield "00045"
            if rand.NextDouble() > 0.5 then yield "00046"
            if rand.NextDouble() > 0.5 then yield "00047"
            if rand.NextDouble() > 0.5 then yield "00048"
            if rand.NextDouble() > 0.5 then yield "00049"
            if rand.NextDouble() > 0.5 then yield "00050"
            if rand.NextDouble() > 0.5 then yield "00051"
            if rand.NextDouble() > 0.5 then yield "00052"
            if rand.NextDouble() > 0.5 then yield "00053"
            if rand.NextDouble() > 0.5 then yield "00054"
            if rand.NextDouble() > 0.5 then yield "00055"
            if rand.NextDouble() > 0.5 then yield "00056"
            if rand.NextDouble() > 0.5 then yield "00057"
            if rand.NextDouble() > 0.5 then yield "00058"
            if rand.NextDouble() > 0.5 then yield "00059"
            if rand.NextDouble() > 0.5 then yield "00060"
            if rand.NextDouble() > 0.5 then yield "00061"
            if rand.NextDouble() > 0.5 then yield "00062"
            if rand.NextDouble() > 0.5 then yield "00063"
            if rand.NextDouble() > 0.5 then yield "00064"
            if rand.NextDouble() > 0.5 then yield "00065"
            if rand.NextDouble() > 0.5 then yield "00066"
            if rand.NextDouble() > 0.5 then yield "00067"
            if rand.NextDouble() > 0.5 then yield "00068"
            if rand.NextDouble() > 0.5 then yield "00069"
            if rand.NextDouble() > 0.5 then yield "00070"
            if rand.NextDouble() > 0.5 then yield "00071"
            if rand.NextDouble() > 0.5 then yield "00072"
            if rand.NextDouble() > 0.5 then yield "00073"
            if rand.NextDouble() > 0.5 then yield "00074"
            if rand.NextDouble() > 0.5 then yield "00075"
            if rand.NextDouble() > 0.5 then yield "00076"
            if rand.NextDouble() > 0.5 then yield "00077"
            if rand.NextDouble() > 0.5 then yield "00078"
            if rand.NextDouble() > 0.5 then yield "00079"
            if rand.NextDouble() > 0.5 then yield "00080"
            if rand.NextDouble() > 0.5 then yield "00081"
            if rand.NextDouble() > 0.5 then yield "00082"
            if rand.NextDouble() > 0.5 then yield "00083"
            if rand.NextDouble() > 0.5 then yield "00084"
            if rand.NextDouble() > 0.5 then yield "00085"
            if rand.NextDouble() > 0.5 then yield "00086"
            if rand.NextDouble() > 0.5 then yield "00087"
            if rand.NextDouble() > 0.5 then yield "00088"
            if rand.NextDouble() > 0.5 then yield "00089"
            if rand.NextDouble() > 0.5 then yield "00090"
            if rand.NextDouble() > 0.5 then yield "00091"
            if rand.NextDouble() > 0.5 then yield "00092"
            if rand.NextDouble() > 0.5 then yield "00093"
            if rand.NextDouble() > 0.5 then yield "00094"
            if rand.NextDouble() > 0.5 then yield "00095"
            if rand.NextDouble() > 0.5 then yield "00096"
            if rand.NextDouble() > 0.5 then yield "00097"
            if rand.NextDouble() > 0.5 then yield "00098"
            if rand.NextDouble() > 0.5 then yield "00099"
            if rand.NextDouble() > 0.5 then yield "00100"
            if rand.NextDouble() > 0.5 then yield "00101"
            if rand.NextDouble() > 0.5 then yield "00102"
            if rand.NextDouble() > 0.5 then yield "00103"
            if rand.NextDouble() > 0.5 then yield "00104"
            if rand.NextDouble() > 0.5 then yield "00105"
            if rand.NextDouble() > 0.5 then yield "00106"
            if rand.NextDouble() > 0.5 then yield "00107"
            if rand.NextDouble() > 0.5 then yield "00108"
            if rand.NextDouble() > 0.5 then yield "00109"
            if rand.NextDouble() > 0.5 then yield "00110"
            if rand.NextDouble() > 0.5 then yield "00111"
            if rand.NextDouble() > 0.5 then yield "00112"
            if rand.NextDouble() > 0.5 then yield "00113"
            if rand.NextDouble() > 0.5 then yield "00114"
            if rand.NextDouble() > 0.5 then yield "00115"
            if rand.NextDouble() > 0.5 then yield "00116"
            if rand.NextDouble() > 0.5 then yield "00117"
            if rand.NextDouble() > 0.5 then yield "00118"
            if rand.NextDouble() > 0.5 then yield "00119"
            if rand.NextDouble() > 0.5 then yield "00120"
            if rand.NextDouble() > 0.5 then yield "00121"
            if rand.NextDouble() > 0.5 then yield "00122"
            if rand.NextDouble() > 0.5 then yield "00123"
            if rand.NextDouble() > 0.5 then yield "00124"
            if rand.NextDouble() > 0.5 then yield "00125"
            if rand.NextDouble() > 0.5 then yield "00126"
            if rand.NextDouble() > 0.5 then yield "00127"
            if rand.NextDouble() > 0.5 then yield "00128"
            if rand.NextDouble() > 0.5 then yield "00129"
            if rand.NextDouble() > 0.5 then yield "00130"
            if rand.NextDouble() > 0.5 then yield "00131"
            if rand.NextDouble() > 0.5 then yield "00132"
            if rand.NextDouble() > 0.5 then yield "00133"
            if rand.NextDouble() > 0.5 then yield "00134"
            if rand.NextDouble() > 0.5 then yield "00135"
            if rand.NextDouble() > 0.5 then yield "00136"
            if rand.NextDouble() > 0.5 then yield "00137"
            if rand.NextDouble() > 0.5 then yield "00138"
            if rand.NextDouble() > 0.5 then yield "00139"
            if rand.NextDouble() > 0.5 then yield "00140"
            if rand.NextDouble() > 0.5 then yield "00141"
            if rand.NextDouble() > 0.5 then yield "00142"
            if rand.NextDouble() > 0.5 then yield "00143"
            if rand.NextDouble() > 0.5 then yield "00144"
            if rand.NextDouble() > 0.5 then yield "00145"
            if rand.NextDouble() > 0.5 then yield "00146"
            if rand.NextDouble() > 0.5 then yield "00147"
            if rand.NextDouble() > 0.5 then yield "00148"
            if rand.NextDouble() > 0.5 then yield "00149"
            if rand.NextDouble() > 0.5 then yield "00150"
            if rand.NextDouble() > 0.5 then yield "00151"
            if rand.NextDouble() > 0.5 then yield "00152"
            if rand.NextDouble() > 0.5 then yield "00153"
            if rand.NextDouble() > 0.5 then yield "00154"
            if rand.NextDouble() > 0.5 then yield "00155"
            if rand.NextDouble() > 0.5 then yield "00156"
            if rand.NextDouble() > 0.5 then yield "00157"
            if rand.NextDouble() > 0.5 then yield "00158"
            if rand.NextDouble() > 0.5 then yield "00159"
            if rand.NextDouble() > 0.5 then yield "00160"
            if rand.NextDouble() > 0.5 then yield "00161"
            if rand.NextDouble() > 0.5 then yield "00162"
            if rand.NextDouble() > 0.5 then yield "00163"
            if rand.NextDouble() > 0.5 then yield "00164"
            if rand.NextDouble() > 0.5 then yield "00165"
            if rand.NextDouble() > 0.5 then yield "00166"
            if rand.NextDouble() > 0.5 then yield "00167"
            if rand.NextDouble() > 0.5 then yield "00168"
            if rand.NextDouble() > 0.5 then yield "00169"
            if rand.NextDouble() > 0.5 then yield "00170"
            if rand.NextDouble() > 0.5 then yield "00171"
            if rand.NextDouble() > 0.5 then yield "00172"
            if rand.NextDouble() > 0.5 then yield "00173"
            if rand.NextDouble() > 0.5 then yield "00174"
            if rand.NextDouble() > 0.5 then yield "00175"
            if rand.NextDouble() > 0.5 then yield "00176"
            if rand.NextDouble() > 0.5 then yield "00177"
            if rand.NextDouble() > 0.5 then yield "00178"
            if rand.NextDouble() > 0.5 then yield "00179"
            if rand.NextDouble() > 0.5 then yield "00180"
            if rand.NextDouble() > 0.5 then yield "00181"
            if rand.NextDouble() > 0.5 then yield "00182"
            if rand.NextDouble() > 0.5 then yield "00183"
            if rand.NextDouble() > 0.5 then yield "00184"
            if rand.NextDouble() > 0.5 then yield "00185"
            if rand.NextDouble() > 0.5 then yield "00186"
            if rand.NextDouble() > 0.5 then yield "00187"
            if rand.NextDouble() > 0.5 then yield "00188"
            if rand.NextDouble() > 0.5 then yield "00189"
            if rand.NextDouble() > 0.5 then yield "00190"
            if rand.NextDouble() > 0.5 then yield "00191"
            if rand.NextDouble() > 0.5 then yield "00192"
            if rand.NextDouble() > 0.5 then yield "00193"
            if rand.NextDouble() > 0.5 then yield "00194"
            if rand.NextDouble() > 0.5 then yield "00195"
            if rand.NextDouble() > 0.5 then yield "00196"
            if rand.NextDouble() > 0.5 then yield "00197"
            if rand.NextDouble() > 0.5 then yield "00198"
            if rand.NextDouble() > 0.5 then yield "00199"
            if rand.NextDouble() > 0.5 then yield "00200"
        ]
